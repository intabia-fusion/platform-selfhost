PLATFORM_VERSION=${PLATFORM_VERSION}
# Registry/namespace for platform images. elastic-plugins is not mirrored, it stays on Docker Hub.
IMAGE_PREFIX=${IMAGE_PREFIX}
DESKTOP_CHANNEL=${DESKTOP_CHANNEL}
DOCKER_NAME=${DOCKER_NAME}
DEV_MODE=${DEV_MODE}

# The address of the host or server from which you will access your Platform instance.
# This can be a domain name (e.g., platform.example.com) or an IP address (e.g., 192.168.1.1).
HOST_ADDRESS=${HOST_ADDRESS}

# Set this variable to 'true' to enable SSL (HTTPS/WSS).
# Leave it empty to use non-SSL (HTTP/WS).
SECURE=${SECURE}

# Specify the IP address to bind to; leave blank to bind to all interfaces (0.0.0.0).
# Do not use IP:PORT format in HTTP_BIND or HTTP_PORT.
HTTP_PORT=${HTTP_PORT}
HTTP_BIND=${HTTP_BIND}

# Platform specific variables
TITLE="${TITLE}"
DEFAULT_LANGUAGE=${DEFAULT_LANGUAGE}
LAST_NAME_FIRST=${LAST_NAME_FIRST}

# PostgreSQL variables
POSTGRES_DB=${POSTGRES_DB}
POSTGRES_USER=${POSTGRES_USER}
POSTGRES_PASSWORD=${POSTGRES_SECRET}
CR_DB_URL=postgres://${POSTGRES_USER}:${POSTGRES_SECRET}@postgres:5432/${POSTGRES_DB}

# Redpanda variables
REDPANDA_ADMIN_USER=${REDPANDA_ADMIN_USER}
REDPANDA_ADMIN_PWD=${REDPANDA_SECRET}

# Docker volume paths - persistent data storage locations
# By default, data is stored in ./data/ subdirectories
# Set to empty to use Docker named volumes instead
VOLUME_ELASTIC_PATH=${VOLUME_ELASTIC_PATH}
VOLUME_FILES_PATH=${VOLUME_FILES_PATH}
VOLUME_POSTGRES_DATA_PATH=${VOLUME_POSTGRES_DATA_PATH}
VOLUME_REDPANDA_PATH=${VOLUME_REDPANDA_PATH}
VOLUME_MAILPIT_PATH=${VOLUME_MAILPIT_PATH}

# Mailpit web UI port (for viewing captured emails)
MAILPIT_HTTP_PORT=${MAILPIT_HTTP_PORT}

# The following configs are auto-generated by the setup script.
# Please do not manually overwrite.
SECRET=${PLATFORM_SECRET}
STORAGE_CONFIG="${STORAGE_CONFIG}"

LIVEKIT_HOST=${LIVEKIT_HOST}
LIVEKIT_API_KEY=${LIVEKIT_API_KEY}
LIVEKIT_API_SECRET=${LIVEKIT_API_SECRET}
LIVEKIT_ENABLED=${LIVEKIT_ENABLED}
LIVEKIT_TURN_DOMAIN=${LIVEKIT_TURN_DOMAIN}

# AI Bot (ЮляИИ) configuration. Run ./setup_aibot.sh to change it interactively:
# it rewrites the values below and regenerates config/config-aibot.yaml, the model
# registry the bot actually routes by (levels -> providers/models).
#
# LLM_MODE: local (OpenAI-compatible server on this host), openai (cloud), gigachat, none
LLM_MODE=${LLM_MODE}
LLM_PROVIDER=${LLM_PROVIDER}
OPENAI_API_KEY=${OPENAI_API_KEY}
OPENAI_BASE_URL=${OPENAI_BASE_URL}
OPENAI_MODEL=${OPENAI_MODEL}
OPENAI_SUMMARY_MODEL=${OPENAI_SUMMARY_MODEL}
OPENAI_TRANSLATE_MODEL=${OPENAI_TRANSLATE_MODEL}
# GigaChat: authorization key is base64 "client_id:client_secret" (built by setup_aibot.sh)
GIGACHAT_AUTH_KEY=${GIGACHAT_AUTH_KEY}
GIGACHAT_SCOPE=${GIGACHAT_SCOPE}
GIGACHAT_CLIENT_ID=${GIGACHAT_CLIENT_ID}
# Quality level requested by default; the catalog lives in config/config-aibot.yaml
AI_DEFAULT_LEVEL=${AI_DEFAULT_LEVEL}

# Transcription (meetings, voice notes) - an OpenAI-compatible /v1/audio/transcriptions endpoint
# STT_MODE: local | openai | none. STT_PROVIDER is the pod-level opt-out switch ('none' disables).
STT_MODE=${STT_MODE}
STT_PROVIDER=${STT_PROVIDER}
STT_URL=${STT_URL}
STT_API_KEY=${STT_API_KEY}
STT_MODEL=${STT_MODEL}

# Payments and subscriptions
# PAYMENT_PROVIDER: mock (plans activate instantly, no bank), tbank, polar, stripe.
# With tbank, start the stack with the `tbank` compose profile; TBANK_MOCK=true keeps the
# bank side in-process (payment links and CONFIRMED webhooks are issued locally).
PAYMENT_PROVIDER=${PAYMENT_PROVIDER}
PAYMENT_ALLOW_MOCK=${PAYMENT_ALLOW_MOCK}
PAYMENT_SANDBOX=${PAYMENT_SANDBOX}
TBANK_MOCK=${TBANK_MOCK}
# Free tier limits; must match the `free: true` plan in plan-config.yaml
FREE_PLAN_LIMITS=${FREE_PLAN_LIMITS}

# Webhooks (debug pair): 'true' starts the webhook service together with webhook-mock
# (UI at /_webhook-mock/, plain-http delivery to it allowed). Leave empty on a public install.
WEBHOOK_ENABLED=${WEBHOOK_ENABLED}

# Push notification keys for notification service
PUSH_PUBLIC_KEY=${PUSH_PUBLIC_KEY}
PUSH_PRIVATE_KEY=${PUSH_PRIVATE_KEY}
